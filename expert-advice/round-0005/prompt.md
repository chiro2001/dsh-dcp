# Round 0005 prompt — M7.0 收尾、rc.4/发布决策与 E7-CODE 取舍

你是 dsh-dcp 项目的顶级审阅/顾问。round-0004 的 M7.0 核心已落地：signed ledger、
真实 storage-domain 接线、`/dcp stats` domain 展示、失败降级与追平测试。当前是
“收尾并决定下一步发布/实验”的节点。

请阅读 `context.md` 及其中列出的输入，完成以下任务。

## 1. 审阅职责

1. **反驳与批评**：对以下判断给出独立意见——
   - M7.0 是否已达到可发布状态，还缺哪些必做项；
   - 是否应发 `v0.1.0-rc.4`（M7.0 切片）还是等 E7-CODE；
   - E7-CODE（Code Mode 完整支持可行性实验）现在并行还是延后；
   - npm 决策是否维持 NO-GO；
   - 探针 oracle/可靠性结论是否已足够支撑发布声明。
2. **最可能遗漏的风险**：真实 JSON whole-file rewrite 规模影响、跨进程口径、
   schema 迁移、rc.4 发布面、CI/安装一致性。
3. **按信息增益排序的 1–3 个“收尾动作/实验”**：输入、动作、可证伪断言、
   停止条件。
4. **方向性判断**：最终应给“v0.2 最小核心已交付”下一个什么结论；下一步建议。
5. **明确区分**：事实 / 推断 / 需实验。

## 2. 交付物

1. **`recommendation.md`**：rc.4 收尾清单 + 发布决策 + 下一步建议；
2. 最终回复由 CLI `-o` 落盘 `response.md`。

## 3. 约束

- 只写 `expert-advice/round-0005/` 目录。
- 不执行安装/构建/测试/git 写操作；不查看既有 TUI 前端插件项目与无关仓库。
